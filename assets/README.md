# assets/

Images, figures, and supporting files used by pages on this site. Keep
the directory shallow and prefer descriptive filenames over generic ones
(`distribution-shape-skew.png`, not `figure-3.png`).
